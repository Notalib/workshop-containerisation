name: build & publish

on:
  workflow_dispatch:
  push:
    branches: ["main"]

permissions:
  packages: write

env:
  REGISTRY: ghcr.io/notalib

jobs:
  buildGoApp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4.0.0
      - name: Setup docker buildx
        uses: docker/setup-buildx-action@v4.0.0
        with:
          platforms: linux/amd64,linux/arm64
      - name: Login to registry ghcr.io
        uses: docker/login-action@v3.0.0
        with:
          registry: ${{ env.REGISTRY }}
          username: nota-ci
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build micro-go-app
        run: docker buildx bake micro-go-app --pull --push
  # buildGoAppFromTemplate:
  #   needs: buildGoApp
  #   uses: notalib/gh-workflows/.github/workflows/docker-buildx-publish.yml@main
  #   with:
  #     context: "./micro-go-app"
  #     tagLatest: ${{ github.ref == format('refs/heads/{0}', 'main') }}
  #     imagePath: "${{ github.repository }}/workshop-containerisation/micro-go-app"
  #   secrets:
  #     registryToken: ${{ secrets.GITHUB_TOKEN }}
